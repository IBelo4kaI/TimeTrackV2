ADDR = "0.0.0.0:8080"
GRPC_ADDR = 'localhost:8383'
DB_STRING = 'root:qwerty@tcp(localhost)/time_track_v2?parseTime=true&charset=utf8mb4&loc=UTC'
FRONTEND_URL = 'https://timetrack.st29.ru'

VK_GROUP_ID = ''
VK_GROUP_TOKEN = ''
VK_CONFIRMATION_STRING = ''
VK_SECRET_KEY = ''
VK_COMMUNITY_SCREEN_NAME = ''

# Сервис авторизации (см. internal/authservice) — получение информации о
# пользователях по HTTP. API-ключ уходит как cookie "session".
AUTH_SERVICE_HOST = 'http://localhost:8382'
AUTH_SERVICE_API_KEY = ''

# Сами адрес/порт/логин/пароль SMTP настраиваются в "Настройках" (см.
# internal/smtp_settings), не тут — тут только ключ шифрования пароля в БД
# (AES, любая непустая строка). Если пустой — сохранить пароль будет нельзя.
SMTP_ENCRYPTION_KEY = ''
